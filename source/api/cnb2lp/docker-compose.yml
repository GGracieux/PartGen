version: '2'

services:
    cnb2lp-web:
        image: nginx:latest
        ports:
            - "8080:80"
        volumes:
            - ./api:/api
            - ./nginx.conf:/etc/nginx/conf.d/default.conf
        networks:
            - api-network
    cnb2lp-php:
        image: php:fpm
        volumes:
            - ./api:/api
            - ./php-log.conf:/usr/local/etc/php-fpm.d/zz-log.conf
        networks:
            - api-network

networks:
    api-network:
        driver: bridge
